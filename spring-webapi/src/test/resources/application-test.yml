spring:
  datasource:
    url: jdbc:h2:mem:tribe-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  jackson:
    property-naming-strategy: SNAKE_CASE

app:
  security:
    jwt:
      secret: test-secret-key-change-me-please-1234567890
      access-token-ttl-seconds: 900
      refresh-token-ttl-seconds: 604800
